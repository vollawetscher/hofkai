# ELO Office Import für Windows

## Voraussetzungen

1. **Python 3.8+** installiert
2. **Microsoft Access Treiber** (bereits in Windows enthalten)
3. **Zugriff** auf `C:\Program Files (x86)\ELOoffice`

## Installation

```bash
# In scripts/ Ordner navigieren
cd scripts

# Dependencies installieren
pip install -r requirements.txt
